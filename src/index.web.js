import {AppRegistry} from 'react-native';
import App from './App';

AppRegistry.registerComponent('ReactNativeAcademy', () => App);

AppRegistry.runApplication('ReactNativeAcademy', {
  rootTag: document.getElementById('root'),
});
